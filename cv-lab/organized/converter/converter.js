CvLab.converter = function(){
	/**
	 * Contains methods that convert data amoung various pixel formats and
	 * colorspaces.
	 * 
	 * All conversion functions take a single argument: a pixel and return
	 * a copy of that pixel in an alternate format or color space. A pixel
	 * is an array with 4 elements each element between 0 and 255.
	 * 
	 * You can convert all of the pixels in an image using
	 * `CvLab.image.convert` and passing it one of the `CvLab.converter`
	 * functions.
	 */
};

