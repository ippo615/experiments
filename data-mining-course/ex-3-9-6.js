/**
 * Exercise 3.9.6 : Suppose we use indexes based on both position and
 * suffix length, as in Section 3.9.6. If s is a string of length 20,
 * with what symbol-position-length triples will s be compared with,
 * if (a) J = 0.8 (b) J = 0.9?
 */

// NOT DONE
