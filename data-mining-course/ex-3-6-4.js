/**
 * Exercise 3.6.4 : Generalize Exercise 3.6.3 to give, as a function
 * of r and b, the  point of maximum slope and the value of that slope,
 * for families of functions defined from the minhash functions by:
 * 
 * (a) An r-way AND construction followed by a b-way OR construction.
 * (b) A b-way OR construction followed by an r-way AND construction.
 * 
 */

// NOT DONE

// My solution to 3-6-3 can compute the point and value of max slope 
// for ANY or/and contrustion but it does it numerically. Perhaps I'll
// do it symbolically (someday).
