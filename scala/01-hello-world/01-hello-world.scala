object HelloWorld extends App {
	println("Hello, world!")
}

// Compile
//   scalac 01-hello-world.scala

// Run (NOTE: `scalac` automatically creates/names files based on the
// code, you cannot rename the files otherwise when you try to run it
// you will get a `java.lang.NoClassDefFoundError`.
//   scala HelloWorld
