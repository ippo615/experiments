'''
Consider the following Python expression: len(set(text4)). State the
purpose of this expression. Describe the two steps involved in
performing this computation.
'''

print 'The purpose of `len(set(text4))` is to return the number of unique tokens in `text4`.'
print 'The first step `set(text4)` is to create a list of unique words.'
print 'The second step `len(...)` is to count the number of words.'
